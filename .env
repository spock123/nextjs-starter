# DO NOT ADD SECRETS TO THIS FILE. This is a good place for defaults.
# If you want to add secrets use `.env.local` instead.

# Remember: publically exposed environment variables must start with NEXT_PUBLIC to be visible

ENV_VARIABLE="I am server-only secret"
NEXT_PUBLIC_ENV_VARIABLE="I am a public secret"